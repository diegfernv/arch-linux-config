#!/usr/bin/env bash
RASI="~/.config/hypr/rofi/styles/launcher.rasi"

rofi_cmd() {
	rofi -no-config \
        -no-lazy-grab \
        -show drun \
        -modi drun \
        -theme $RASI
}

rofi_cmd
